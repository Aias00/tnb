# MCP Checklist

- Confirm the transport contract first: stdio, HTTP, or SSE.
- Separate process-launch failures from protocol handshake failures.
- Verify declared capabilities match the user goal: tools, prompts, resources, auth, or sampling.
- Document which values come from files versus environment variables.
