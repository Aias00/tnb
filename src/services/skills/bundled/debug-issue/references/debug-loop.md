# Debug Loop

- Start with one concrete failing symptom, not a broad theory.
- Capture the exact command, input, and output that distinguish broken from correct behavior.
- Change one variable at a time while testing a hypothesis.
- Preserve the shortest reproduction you can find so the fix stays provable.
- Before closing, run the original failing scenario and at least one adjacent regression check.
