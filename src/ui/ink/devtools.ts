// React DevTools integration is optional. The renderer dynamically imports
// this module only in development builds, so the standalone renderer keeps
// a stable import boundary without requiring a debug server.
export {}
